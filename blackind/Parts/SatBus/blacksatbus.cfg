PART {
	// --- general parameters ---
	name = blacksatbus
	module = Part
	author = Blackleg

	// --- asset parameters ---
	mesh = blacksatbus.mu
	scale = 1

 	// --- node definitions ---
  	// definition format is Position X, Position Y, Position Z, Up X, Up Y, Up Z
	//node_stack_bottom = x, y, z, angx, angy, angz, size
	//node_stack_top = x, y, z, angx, angy, angz, size
	node_stack_bottom = 0, 0.5, 0, 0, 1.0, 0, 0
	node_stack_top = 0, -0.5, 0, 0, -1.0, 0, 0

	// --- editor parameters ---
	TechRequired = electronics
	entryCost = 500
	cost = 1500
	category = Pods
	subcategory = 0
	title = SatBus A
	manufacturer = BlackLeg Industries
	description = BlackLeg Industries Sat Bus Type A

	// attachment rules: stack, srfAttach, allowStack, allowSrfAttach, allowCollision
	attachRules = 0,0,1,1,0

	// --- standard part parameters ---
	mass = 1
	dragModelType = default
	maximum_drag = 0.2
	minimum_drag = 0.15
	angularDrag = 0.2
	crashTolerance = 5
	maxTemp = 400

	RESOURCE {
		name = ElectricCharge
		amount = 200
		maxAmount = 200
	}

}

